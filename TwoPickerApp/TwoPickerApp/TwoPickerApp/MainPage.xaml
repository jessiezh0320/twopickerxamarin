<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TwoPickerApp"
             x:Class="TwoPickerApp.MainPage">

    <StackLayout>
        <Picker Title="select city" x:Name="CityPicker" ItemsSource="{Binding Cities}" ItemDisplayBinding="{Binding CityName}" SelectedIndexChanged="DiameterPicker_SelectedIndexChanged"></Picker>
        <Picker Title="select region" x:Name="RegionPicker" ItemDisplayBinding="{Binding RegionName}"></Picker>
    </StackLayout>

</ContentPage>
